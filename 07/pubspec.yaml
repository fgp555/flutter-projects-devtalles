name: dev
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.1

dependencies:
  animate_do: ^5.1.0
  flutter:
    sdk: flutter
  intl: ^0.20.2
  provider: ^6.1.5+1
  video_player: ^2.9.2
  # video_player: ^2.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/videos/
